# MySQL Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=mini_dmart
DB_USER=root
DB_PASSWORD=

# JWT Secret Configuration (Use a strong base64 encoded key in production)
JWT_SECRET=super_secret_key_for_development_environment_only
JWT_EXPIRATION_MS=86400000

# Stripe Configuration
STRIPE_SECRET_KEY=your_stripe_test_secret_key
